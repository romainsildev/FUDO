# Projet : FUDO — Monk Mode Challenge

> Ce fichier est la mémoire persistante de Claude Code. Le lire avant chaque prompt. Le tenir à jour (voir Carnet de notes en bas).

## Concept

App iOS native de discipline gamifiée : l'user entre en **monk mode** — un défi à durée fixe (30/60/90 j) où il valide chaque jour ses non-négociables. Sa constance fait monter son **OVR** (0-99, type NBA2K) et évoluer son **sensei** à travers 6 rangs. Rater un jour fait chuter l'OVR, pas recommencer le défi. L'app ne bloque rien : la contrainte est psychologique (OVR qui chute, streak cassée, rang perdu).

Persona : homme 16-25, TikTok self-improvement, cluster monk mode / lock in / discipline.

## Stack (figée — ne jamais dévier)

- Swift / SwiftUI, **iOS 17+** (`@Observable`, SwiftData)
- **SwiftData** pour toute persistance (100 % local, App Group partagé avec le widget)
- **WidgetKit** (widget small + medium)
- **RevenueCat** (subscriptions, trial-first) + **PostHog** (analytics, host EU, wrapper `Analytics.swift`, zéro capture en DEBUG) — les DEUX seules dépendances tierces autorisées
- **UNUserNotificationCenter** : notifications locales uniquement, zéro push serveur
- **AUCUN backend, AUCUNE auth, AUCUN compte, AUCUN réseau** (hors RevenueCat et PostHog). Ne jamais en proposer.
- EN only : toutes les UI strings en anglais (via `Localizable.xcstrings`, base EN, pas d'autre langue)

## Design

- **L'app EST dark** : pas de light mode, `.preferredColorScheme(.dark)` forcé. Pas de logique dark/light.
- Palette ACTÉE (SUMI conservée sous la DA cartoon, décision 2026-07-11 — tokens dans `Core/DesignSystem/Colors.swift`, ne jamais hardcoder un hex dans une view ; doubler chaque couleur dans l'Asset Catalog, mêmes noms, pour le widget) :
  - Fonds : `bgPrimary` #121110 (noir encre chaud) · `bgCard` #1C1A17 · `border` #2A2724 (bordure 1 px sur TOUTES les cards, jamais d'ombre)
  - Texte (jamais de blanc pur) : `textPrimary` #FAF0E6 · `textSecondary` #A89F92
  - Vermillon (10 % max de l'écran) : `accent` #E34234 (CTA, rings, flamme, ensō, barres) · `accentPressed` #FF5140 · `accentDeep` #7A1F17 (fonds badges rang)
  - Deltas OVR : `positive` #34C759 · `negative` #FF453A — la FLÈCHE ▲/▼ porte le vert/rouge, les barres/rings restent vermillon (décision 2026-07-11). **Exception actée 2026-07-12 : `positive` aussi autorisé pour les badges de validation/reco** (ex. "Recommended for you") — le vert reste INTERDIT partout ailleurs en accent UI.
  - `celebrationGold` #E8B44A : réservé aux bursts de célébration (journée 100 %, rank-up)
  - Interdits absolus : violet, bleu électrique, vert en accent UI, dégradés néon (territoires des 4 concurrents + signe d'app vibe-codée)
- Spacing/radius : marges écran 20 · padding card 16 (20 pour les majeures) · inter-sections 40 · radius card 24 `.rect(style: .continuous)` (nested 8) · CTA = Capsule hauteur 56 · rings 6 pt lineCap `.round`
- Fonts : SF Pro Display (titres, OVR géant en `.monospacedDigit()`), SF Pro Text (body). **Bebas Neue** (TTF embarqué `Resources/Fonts/`, déclaré UIAppFonts) = hooks display ONBOARDING uniquement, jamais dans l'UI app.
- **Animations 0.4-0.6 s ease-in-out** partout (lent = premium). Jamais rapides/sèches.
- Haptics : boutons principaux, validations (hold-to-check progressif), transitions onboarding — **nulle part ailleurs**.
- Confetti/célébrations réservés aux milestones : journée 100 %, montée de rang, fin de défi. Le reste = signes discrets.
- Chaque écran : états success / loading / failure pensés. Loading quasi nul (local) SAUF paywall (fetch produits) et partage → loading + failure avec retry obligatoires. **Jamais d'écran vide** (empty state = CTA de relance).
- Ton copy : direct, 2e personne, dur-mais-satisfait, viril sans caricature. Factuel sur l'échec ("Yesterday: incomplete. OVR -4."), jamais culpabilisant-verbeux.
- Dossier `DesignReference/` (hors build) : screenshots d'inspiration — le consulter pour toute tâche visuelle.

## Conventions

- MVVM léger : `@Observable` (jamais `ObservableObject`), un ViewModel par feature, pas par sous-view.
- Un fichier = une view. Organisation par feature : `FUDO/Features/<Feature>/` (le ViewModel à la racine, les views dans `Views/`) ; le transverse vit dans `FUDO/Core/` (`DesignSystem`, `Game`, `Models`, `Navigation`, `Services`, `Extensions`) et la coquille dans `FUDO/App/`.
- `NavigationStack` (jamais `NavigationView`). **TabView NATIF 4 onglets** (Today · Progress · Stats · Settings) — Liquid Glass système sur iOS 26, barre standard avant ; JAMAIS de tab bar custom (décision 2026-07-12, pattern RiteOff). Masquage en push = `.fudoHidesTabBar()` (= `.toolbar(.hidden, for: .tabBar)`). Tint barre = textPrimary, contenu re-tinté accent. Conventions nav (prd/12 §1) : **tab switch** (jamais de push cross-onglet) · **sheet** medium (flamme, time picker, share) · **push** (tab bar MASQUÉE : habit detail, sous-écrans Settings) · **cover** (onboarding, paywall, setup standalone, challenge complete, rank-up) · **alert ×2** destructif. Onboarding/paywall/covers en `.fullScreenCover`.
- Noms en anglais (code + fichiers). UI strings en anglais.
- Pas de force unwrap (`!`), pas de `try!` sur les opérations SwiftData — toujours gérer l'erreur.
- **Toute constante de gameplay vit dans `Core/Game/GameConfig.swift`** (taux OVR, pénalités, decay, grace period, caps). Jamais de nombre magique dans une view ou un service.
- **`OVREngine` est la source de vérité unique** de la formule OVR (voir `DATA-MODEL.md`) : la projection d'onboarding, le Home, le rollover et le widget appellent le même moteur. Ne jamais dupliquer un calcul OVR.
- **Un nom affiché dérivé d'un type Core vit sur le type Core** (ex. `Rank.displayName`), jamais en helper local de feature : la 2e feature le duplique, la 3e le fait diverger (constaté en S5 — "NOVICE" vs "Novice").
- Widget : ne touche JAMAIS SwiftData — il lit un `WidgetSnapshot` JSON (Codable) écrit dans UserDefaults App Group par `WidgetBridge` côté app (modèle P7).

## Features du MVP (liste exhaustive — ne RIEN builder d'autre)

1. **Défi à durée fixe** avec presets modifiables (Monk Mode 30 / 60 / Hardcore 90 / Monk Mode 120 — acté 2026-07-16 : chips 30/60/90/120, reco = 60, Classic 75 retiré du catalogue mais la case `classic75` survit pour les anciens défis). 1 seul défi actif. Règles éditables jusqu'à J3, verrouillées ensuite.
2. **Checklist quotidienne** hold-to-check 1,5 s (anneau + haptic progressif + burst + delta OVR flottant). Décocher = reprise exacte des points. Anti-farming (plafond OVR/jour).
3. **OVR 0-99 + 6 rangs sensei** (Novice → Sensei), persiste entre les défis, decay d'inactivité léger.
4. **Widget** (small : streak + OVR + jour X/Y ; medium : + 3 tâches restantes) + **notifs locales** (rappel quotidien conditionnel, soir, streak en danger, J-1 trial, decay, montée de rang — cap 2/jour).
5. **Carte de partage 9:16** (sensei + OVR + rang + streak + jour X/Y + branding) — rendu SwiftUI → UIImage → share sheet.
7. **Onboarding** (S5, livré — plan `docs/ONBOARDING-PLAN.md`) : tunnel 25 écrans OB 00→21 en 5 actes, ambiance vidéo → quiz → stat choc → protocole composé → projection datée → contrat signé au doigt → paywall → trio post-paywall. Kill-safety 2 checkpoints + hold-lock.
6. **Stats** (onglet habitudes, acté 2026-07-11 — prd/10 + prd/12 §5) : segmented 7/30/défi, carte résumé, top/flop, liste habitudes (sparkline + tendance) + push détail habitude (3 tuiles, graph, timeline step-by-step, conseil local). Agrégation locale des `DayLog.checks`, zéro donnée nouvelle. Frontière : Progress = le défi et le rang, Stats = les habitudes.

Hors scope (ne jamais coder, même si "facile") : communauté/leaderboard, heatmap détaillée (v1.1), radar domaines (le champ `domain` existe en base, pas d'UI), rest day (le champ `restDayWeekday` existe en base, pas d'UI — D5), AI coach, journaling, blocage d'apps (v2), auth/sync, localisation, light mode, export de données.

## Règles de gameplay (invariants — ne pas casser)

- Journée = minuit → minuit local, **grace period silencieuse jusqu'à 2 h du matin** (rollover à 2 h).
- Pas de validation rétroactive : hier est mort.
- Streak = journées 100 % consécutives. Incomplète à la clôture → pénalité OVR + streak cassée.
- Anti-farming : somme des deltas de tâches = plafond du jour ; check/uncheck = neutre exact ; aucune tâche ne paie 2×.
- Le rang persiste entre défis. Decay uniquement sans défi actif, plancher = bas du rang courant (on ne perd jamais un rang par decay).
- Trial expiré sans achat → paywall, données conservées (argument de réactivation). User payant → plus JAMAIS de paywall.

## Pièges connus (leçons des sources — tester systématiquement)

- App tuée puis relancée : ne doit PAS redéclencher une notif/animation à tort ; restauration de navigation propre.
- Review prompt : `SKStoreReviewController` natif UNIQUEMENT, jamais de modal custom.
- Paywall : produits qui ne chargent pas = CTA mort = rejet Apple Guideline 2.1 → loading + retry obligatoires. Prix complet + mention renouvellement auto visibles sur l'écran.
- `PrivacyInfo.xcprivacy` requis pour chaque SDK (RevenueCat, PostHog) + APIs "required reason" — rejet automatique sinon.
- Restore purchases fonctionnel + chemin "payant → plus de paywall" testés avant chaque soumission.
- Zéro placeholder, bouton mort ou "coming soon" dans un build soumis.
- SwiftData : vérifier que Claude n'a pas créé des doublons de fichiers modèles ; pas de `try!` silencieux.
- Médias qui débordent : contraindre via `GeometryReader` + `frame`, pas des paddings au hasard.

## Hygiène du repo (obligatoire — leçon du 2026-07-12 : 4 ensō perdus par manipulation d'assets hors process)

- **Source de vérité des assets = CE repo (git).** `~/Downloads` = zone de transit, jamais une source. Ne JAMAIS supprimer/écraser un asset sans avoir vérifié qu'une copie est committée (`git log --follow <fichier>`).
- `Assets.xcassets` : JAMAIS d'image posée en vrac dedans — uniquement des `.imageset`/`.appiconset` avec leur `Contents.json`.
- Tout asset bundlé (image, font, vidéo) vit sous `FUDO/` (dossier synchronisé Xcode) — `FUDO/Resources/Welcome/`, `FUDO/Resources/Fonts/`, `FUDO/Assets.xcassets/`. Rien d'autre à la racine du repo que : `CLAUDE.md`, `.gitignore`, `docs/`, `DesignReference/`, `FUDO/`, `FUDO.xcodeproj/`, `FUDOTests/`, `FUDOUITests/`.
- `DesignReference/` = screenshots des frames Figma (fournis par le chef d'orchestre session par session, sous-dossiers `app/`, `onboarding/`). HORS build (jamais dans un target) — à CONSULTER pour toute tâche visuelle : c'est la maquette de référence, pas une inspiration.
- Pas de fichier temporaire/brouillon dans le repo ; pas de doublons (`Foo 2.swift`) — si Xcode en génère un, le signaler et le résoudre immédiatement.
- `xcuserdata/`, `.DS_Store`, `*.xcuserstate` = ignorés via `.gitignore` — ne jamais les committer.
- Toute suppression de fichier = commande servie à Romain, jamais exécutée en autonomie.
- Fin de session : après le commit de checkpoint, `git status` doit être vide (zéro orphelin non tracké).

## Principes pour Claude

- **RÉPONDS TOUJOURS À ROMAIN EN FRANÇAIS** — rapports, plans, questions, explications, checklists. Romain est vibe codeur, pas développeur : vulgarise (pas de jargon sans une parenthèse d'explication). Seuls le code, les noms de fichiers, les commits et les UI strings restent en anglais.

- **Machine 16 GB (M5 base) — la charge vient des BUILDS, pas des subagents.** Subagents OK pour écrire du code/des fichiers. INTERDIT : deux `xcodebuild` en même temps, quelle que soit la source. Vérif par étape = compile-only SANS booter de simulateur (`xcodebuild build -destination 'generic/platform=iOS Simulator'`). Suite de tests complète (qui boote un sim) : UNE fois par session, jamais par étape, jamais en parallèle d'autre chose. Zéro sim booté en tâche de fond. La vérif visuelle/device = Romain, dans Xcode, sur son iPhone (Cmd+R) — jamais la session.
- Plan mode avant tout build multi-écrans ; **ultrathink pour les tâches visuelles/UI**.
- Si tu modifies une feature, vérifier que les autres marchent encore (surtout OVREngine ↔ Home ↔ widget).
- Ne JAMAIS modifier la formule OVR ou les constantes de `GameConfig` sans accord explicite de Romain.
- Avant de refactorer : demander confirmation.
- Les décisions produit appartiennent à Romain : proposer options + reco 1 ligne, ne rien trancher seul.
- Mettre à jour ce fichier (features, conventions découvertes, carnet) au fil du build.

## Carnet de notes (append-only, tenir à jour à chaque session)

| Date | Note (convention découverte, piège rencontré, décision de build) |
|---|---|
| 2026-07-12 | Session 1 (data layer) : 4 @Model + OVREngine + GameStore + DebugSeed + câblage livrés. Décisions actées : 1 pénalité PAR jour manqué (DayLogs synthétiques au rollover, en ordre) · `OnboardingAnswers` typée (barème dans les enums) · seed = replay moteur base 49 → OVR 61 / streak 4 / J12 · `lastDayClosedAt` = lendemain du jour clos (horloge idle du decay). |
| 2026-07-12 | PIÈGE SwiftData : plusieurs `ModelContainer` créés via la variadique `ModelContainer(for: Type.self, …)` dans un même process (host app + tests unitaires in-memory) → crash SIGTRAP interne SwiftData à l'insert (métadonnées `.unique` dupliquées, iOS 17). Fix : UNE instance `Schema` partagée (`FudoSchema.schema`), tout container se construit dessus. **Fix NON VÉRIFIÉ** (run de tests stoppé par Romain, machine saturée) — à vérifier Cmd+U. OVREngineTests : 100 % verts au run initial. |
| 2026-07-12 | RÈGLE DURCIE : 1 seul `xcodebuild test` par session, MÊME pour debugger un échec — analyse statique (crash logs `~/Library/Logs/DiagnosticReports`) + fix committé non vérifié + vérif par Romain dans Xcode. Fin de session : `pkill -f xcodebuild` + `xcrun simctl shutdown all`. |
| 2026-07-12 | Crash tests persistant malgré le Schema partagé (Cmd+U Romain : EXC_BREAKPOINT à l'insert de PlayerState). Évidence : host + 1 container de test = vert ; crash quand les containers S'ACCUMULENT. Triple fix : (1) FUDOApp = coquille vide sous session de test (env `XCTestSessionIdentifier`/`XCTestConfigurationFilePath`) — ni container réel, ni seed, ni rollover pendant les tests ; (2) suites SwiftData `@Suite(.serialized)` ; (3) `SwiftDataTestSupport.freshContainer()` = UN container in-memory pour tout le process de test, vidé fetch+delete entre tests (`ModelContext.delete(model:)` batch pas fiable en iOS 17). Toute nouvelle suite SwiftData DOIT passer par `SwiftDataTestSupport`, jamais créer son container. Vérif = Cmd+U Romain. |
| 2026-07-12 | Session Home : HomeView 3 états (01/01b/01c) + FlameSheetView + SenseiStageView (hooks pulse/slump/celebrate — l'art réel remplacera `react()`/`celebrate()` et le modifier de posture, call sites intacts). GameStore : agrégats READ-ONLY ajoutés (`effectiveToday`, `displayCalendar`, `todayNumber`, `dayLog(on:)`, `totalChecksAllTime`) — les views ne touchent jamais `Date.now`, toujours l'horloge du store. `HomePlaceholderView` = shim `@Environment(GameStore.self)` → `HomeView(store:)` car MainTabView hors scope session — renommer le call site au prochain passage shell puis supprimer le shim. Célébration 100 % = one-shot via triggers en mémoire du VM (relaunch ne rejoue RIEN, piège connu). Cover challengeSetup = stub avec bouton close temporaire (le vrai flow possédera ses sorties). Compile-only vert ; vérif visuelle = Romain (Cmd+R). |
| 2026-07-12 | Session hold-to-check : `HoldToConfirm` (Core/DesignSystem) = geste signature réutilisable — Button + ButtonStyle `isPressed` (le ScrollView annule la pression nativement au drag vertical → ring rewind 0,3 s, zéro combat de gestures), horloge = Task annulable (source de vérité du confirm, exactly-once via `sealed`), ring = trim animé sur la shape passée en param. Haptique progressive light→medium→heavy aux quarts + `.success` au seal (`.heavy` dispo pour la confirmation de défi). Constantes dans `HoldToConfirmMetrics` + `ParticleBurstMetrics` (jamais dans GameConfig — gameplay only). `ParticleBurstView` (DesignSystem) généralise le burst ; `GoldBurstView` = preset gold par-dessus. Row : seal echo + burst + "+X OVR" flottant au niveau ROW (survivent au flip checked) ; delta affiché = relecture du `TaskCheck.ovrDelta` enregistré (zéro calcul dupliqué). Uncheck = long-press 0,5 s → confirmationDialog destructif, refund exact, aucun burst. Compile-only vert ; feel du geste = vérif Romain sur device. |
| 2026-07-12 | Session rollover UI : scene-active déjà câblé (RootView.refresh, session 1) — ajouté le timer foreground : `GameStore.timeUntilNextRollover` (borne = minuit effectif + graceHours, horloge du store) + `HomeViewModel.watchRolloverWhileForeground()` en boucle `.task` sur HomeView (idempotent, double-fire scene/timer inoffensif). ATTENTION Task.sleep = continuous clock : un saut d'horloge machine ne déclenche PAS le timer en cours — pour tester, régler l'horloge AVANT de lancer l'app. Banner "Yesterday: incomplete. OVR -X." : métrique = PÉNALITÉ du closure (`checksTotal − ovrDelta`), pas le net du jour (les gains live étaient déjà affichés la veille) — badge OVR aligné sur la même métrique. Multi-jours manqués → "N days incomplete. OVR -Σ." (à valider Romain). Dismiss persisté par jour effectif dans UserDefaults (`home.incompleteBanner.dismissedDay`) — relaunch ne re-montre pas un banner fermé. |
| 2026-07-12 | Polish pass Home (device run Romain) : hold 1,5→1,0 s (`HoldToConfirmMetrics.duration`, courbe haptique auto-compressée) · uncheck 0,35 s (`quickLongPress`) · header PINNED + hero collapsant (interpolation DIRECTE sur offset scroll via PreferenceKey — jamais de withAnimation sur le scroll, réversible 60 fps ; constantes `HomeHeroMetrics` dans HomeView.swift) → strip compact 44 pt (tête + OVR·RANG + mini ring) en overlay glass · avatar = crop PIXEL (`SenseiAssetProvider.headImage`, fractions 0.38 w / 0.07 h, cache @MainActor) — plus jamais de crop par offset/scale SwiftUI · `FudoGlassCapsule` = 2 chemins : `glassEffect` natif iOS 26 (SDK OK, compilé) / recette material fallback — tous les call sites upgradés gratuitement · flamme = `FudoGradient.flame` (gold→vermillon, identité de la flamme, PAS un usage célébration du gold) pill + sheet (breathing 1,8 s = exception assumée à la règle 0,4-0,6 s) · célébration 100 % = séquence 1,8 s dans `CelebrationMetrics` (seal flash ring + burst gold+vermillon DEPUIS le ring via `originRadius` ParticleBurstView + medium→success). GoldBurstView conservé (rank-up futur). Compile-only vert. |
| 2026-07-12 | Session ChallengeSetup : `ChallengeSetupViewModel` UNIQUE derrière les 3 skins — standalone frame-04 câblé au Home (remplace le stub), full flow 3 écrans construit SANS entrée in-app (l'onboarding hébergera l'inline plus tard). Décisions Romain : Home → standalone uniquement · Day 1 = AUJOURD'HUI (copy frame "Day 1 tomorrow" corrigée, GameStore intact) · valeur des règles cuite dans le title (wake time via picker → string, zéro migration) · reminder standalone = 420 silencieux (picker uniquement en full flow écran 3). Chips 30/60/75/90 ↔ monk30/monk60/classic75/hardcore90 (divergence chips-vs-presets tranchée côté maquette, loguée) ; changer de chip RECHARGE les défauts du preset (édits jetés) ; un preset édité GARDE sa valeur (`.custom` réservé) ; Classic 75 "Progress photo" livrée toggle-OFF = l'optionnel ; ligne "<PRESET> · RECOMMENDED FOR YOU" en `positive` (vert, exception suivant frame 04 — à confirmer Romain). `Challenge.canEditRules(now:)` ajouté (Settings). `ChallengeSetupPlaceholderView.swift` orphelin — suppression à servir à Romain. Compile-only + build-for-testing verts ; Cmd+U = Romain. |
| 2026-07-13 | Fixes device (retours Romain). PIÈGE réutilisable : `startChallenge` exige un `PlayerState` (`guard let player`) — sans joueur il retourne nil EN SILENCE, donc le hold du CTA setup se terminait (anneau + haptique) mais rien ne démarrait. Cause : `debugWipe(reseed:false)` supprimait aussi le joueur → état "no-player" que le Home no-challenge ne recrée jamais. Fix : le wipe vierge recrée un joueur onboardé (OVR base via `OVREngine.startingOVR(from: DebugSeed.answers)`, zéro défi) = vrai frame 01b. En prod le no-challenge a toujours un joueur (fin onboarding/défi), donc invariant OK — seul le chemin debug cassait. Anneau HoldToConfirm invisible sur CTA vermillon → param `ringColor` ajouté (défaut `.accent`), les 2 CTA setup passent `textPrimary` (crème). Header : avatar + 2 pilules forcés à une hauteur commune 34 pt (`HomeHeaderView.elementHeight`) — étaient 32/33/34, décalés près de la Dynamic Island. |
| 2026-07-12 | Fix test + menu DEBUG. PIÈGE test : `EditableRule` Equatable inclut l'`id` (UUID) et `defaultRules` régénère des UUID neufs à chaque appel → JAMAIS comparer des EditableRule par `==` complet dans les tests, toujours champ par champ (title/icon/isEnabled/valueKind/timeMinutes). Menu DEBUG (`DebugMenuSection`, Settings, fichier entier `#if DEBUG`) : Wipe & reseed · Wipe vierge · Complete challenge · Abandon challenge — confirmationDialog, tout passe par GameStore (`debugWipe(reseed:)` + `debugCompleteActiveChallenge()` en extension DEBUG dans GameStore.swift MÊME fichier, accès aux privés ; re-fetch player/activeChallenge après wipe car le seed replay via son propre GameStore). Flag `DebugSeed.seedDisabledKey` (UserDefaults "debug.seedDisabled") : armé par Wipe vierge (le seed auto ne revient pas au relaunch), désarmé par Wipe & reseed. Compile-only vert ; vérif device = Romain. |
| 2026-07-13 | Session S4 Progression : trophy room livrée (8 fichiers Features/Progression/). Hero sensei+OVR géant · sparkline Swift Charts vert/rouge + popover tap · RANK PATH serpentine descendante 6 nodes (discovered high-water / current halo / future silhouette, connecteurs Canvas plein↔pointillé) · past challenges @Query masqués si 0. Décisions Romain : courbe = LIGNE (pas barres maquette) · noms rang EN (pas FR maquette) · hero = sensei (pas anneau seul maquette). PIÈGE dérivations : `OVRPoint` ne stocke NI delta NI complete → delta = value[i]−value[i-1], reach-date = 1er point d'ovrHistory ≥ `Rank.floorOVR`, complete/incomplete = match date → `DayLog.isComplete` (points decay/pré-défi = pas de flag). `Rank` n'a pas de displayName → helper LOCAL Progression (Core hors périmètre). highestRankReached VÉRIFIÉ déjà maintenu (`raiseRankMarkIfNeeded` ← `applyOVRChange` live + `close` rollover) → 0 fix GameStore, juste test de verrou high-water. Tests VM + high-water ajoutés. build-for-testing vert ; Cmd+U + tuning device (hauteur sensei, amplitude serpentine `RankPathMetrics.laneWidth`, silhouette `brightness`) = Romain. |
| 2026-07-13 | Session S4bis Stats : onglet habitudes livré (16 fichiers Features/Stats/). Moteur pur `StatsAggregator` = source unique du calcul stats (StatsView + HabitDetail l'appellent, zéro dup). Décisions Romain : icône = SF Symbol tuilé (pas emoji maquette) · jour EN COURS inclus dans %/compteurs (verdict "missed" = jours clos only) · sans défi actif = dernier run flouté (blur 7) + voile + CTA central "You showed up last run" qui relance via `ChallengeSetupStandaloneView`. Sparkline BINAIRE (habitude/jour binaire ; hauteurs variables maquette = mock). Frontière : aucune ChallengeStatsView existante → rien supprimé. Couleurs : flèche porte vert/rouge, barres vermillon, STRONGEST en `positive` (exception reco), timeline dots vert/rouge = précédent `PointPopover`. Trend = 7 vs 7 ±5 pts, streak/trend indépendants de la période. Shim `StatsPlaceholderView` → `StatsView(store:)`. PIÈGE : nommer un helper `accessibilityLabel(_:)` dans une View entre en collision avec le modifier SwiftUI → renommer (`label(for:)`). `StatsAggregatorTests` ajoutés (% période, top/flop seuil 5j, trend 7v7, today-inclus/missed-closed, streak). Compile-only + build-for-testing verts ; Cmd+U + tuning device (blur, hauteurs barres, copy advice) = Romain. |
| 2026-07-15 | Session S4ter Home V2 re-skin (branche feature/home-v2, frames 01 + 01-collapsed v2). Hero étendu : stage compacté (`HomeHeroMetrics.expandedStageHeight` 264 / ring 248 / sensei 244, sensei CENTRÉ dans l'anneau) + stat line unique sous le stage (RANK vermillon + hint "▸ NEXT · N" | OVR géant 60 | TODAY delta) — les 7 dots/total/best restent dans la flame sheet, l'ancien OVR 84 + rankLine SURVIVENT pour le no-challenge 01b (intact). Strip 44 pt → carte horizontale 112 pt (`collapsedHeroCard`) : mini sensei full-body dans arc de jour + OVR 40 + rang/barre fine `rankProgress`/label "NEXT X · N PTS" + delta — mécanique de collapse S2 réutilisée telle quelle (crossfade + scale 0.96, `cardT`). `SenseiStageView` : cotes paramétrées (défauts = anciennes valeurs → 01b identique), aura proportionnelle au ring. Dérivations rang (`nextRank`/`pointsToNextRank`/`rankProgress`/hints) dans HomeViewModel depuis `Rank.floorOVR` — zéro donnée nouvelle. À Sensei : barre pleine + "MAX RANK". Compile-only vert ; tuning device (tailles stage/stat line, hauteur carte) = Romain. |
| 2026-07-15 | Session S4quater (fix-pass audit, branche feature/audit-fixes). **TYPO** : `FudoFont` n'est plus une fabrique de `Font` mais un RÔLE (poids + ancre Dynamic Type + plafond), la taille reste un paramètre ; 10 tokens (title/headline/body/caption/label/stat/metric/ovr/glyph/onboardingDisplay), 150 call sites passés au modifier `.fudoFont(_:)`, zéro `.system(size:)` hors Typography.swift. PIÈGE tranché : `.system(size:)` n'a PAS de `relativeTo:`, et un `Font` est figé — un pont `UIFontMetrics` rendrait un Font calculé dans le body du parent, or le parent ne se réévalue PAS au changement de Dynamic Type → scaling mort silencieux. Seul `@ScaledMetric` lit `\.dynamicTypeSize` dans l'environnement. Plafonds (Romain) : 1,6× texte / 1,25× chiffres display, `glyph` ÉPINGLÉ (`maxGrowth: 1`) pour tout symbole dimensionné par son conteneur (diamètre d'anneau, frame fixe) — les conteneurs restent FIXES, seul le texte scale. Au réglage par défaut `@ScaledMetric` rend la taille de base → zéro delta visuel (testé). `monospacedDigit` cuit dans stat/metric/ovr. **OVR** : `OVREngine.displayedOVR(_:)` = seule conversion vers l'entier affiché (floor, jamais round) — 4 sites arrondissaient half-up et annonçaient un rang un point trop tôt ; verrou = le rang lu sur l'affiché == le rang lu sur le stocké, balayé 0→99 par 0,25. **PIÈGE test** : ne JAMAIS instancier un `@Model` dans une suite moteur pure (sans container) — passer par `SwiftDataTestSupport`. Paddings 90/100 = héritage de la pilule custom, ils S'AJOUTAIENT au safe-area inset de la TabView native → `FudoSpacing.contentBottom` (24). `ChallengePresetNaming` supprimé (`PresetCatalog.title(for:days:)` = source unique des noms). Push demo gaté `#if DEBUG` (lien + navigationDestination). `CLAUDE.md` ne pointe plus vers `STRUCTURE.md` (jamais dans ce repo, il vit dans le pack de build externe). |
| 2026-07-15 | Session S5 ONBOARDING (branche feature/onboarding, plan `docs/ONBOARDING-PLAN.md`). 25 écrans OB 00→21 + gate paywall, 5 actes. **Décisions Romain** : D1 **bonus révélé** — `commitment` (barème §3a) arrive à OB 16, APRÈS l'OVR d'OB 10 : `OnboardingDraft.answers` le défaute à `.somewhat` (0 pt) → OB 10/13 affichent le PLANCHER, OB 16 réveille le bonus (badge "+N OVR" flottant), le nombre ne DESCEND jamais · D2 **Day 1 = aujourd'hui**, la copy plie ("Day 1 is today", frames "starts tomorrow" fausses) — **piège logué** : onboarder à 21 h = pénalité garantie la nuit 1 (fix futur = `startDay:` sur `startChallenge` + état Home "Day 0") · D3 preset reco **toujours monk30** (tous les hooks promettent "30 DAYS") · D4 **aucune mesure inventée ne ship** : note 4.8 ET étoiles supprimées (des étoiles sans note affirment la même chose), "statistically dead" → "most men are done by day 4", "2× more likely" → non-chiffré ; **RESTE OUVERT** : les 3 témoignages nominatifs OB 15 = placeholders marqués, à remplacer par de vrais retours TestFlight consentis avant submit · D5 **dissolve-loop** (2 `AVPlayerLayer` + fondu sur la couture) au lieu du vrai ping-pong (décodage H.264 arrière = saccades device) · D6 barre cachée sur OB 18 · D7 `PricingCopy` = stub S6. **Architecture** : `OnboardingStep` (enum) = source du total de la barre (JAMAIS numéroté à la main, verrouillé par test) · `ChallengeSetupViewModel` = 4e skin sur OB 11 (zéro dup ; CTA en TAP, pas hold — le défi naît à OB 19) · courbe OB 13 = `OVREngine.project` jour par jour, composant vermillon SÉPARÉ d'`OVRCurveView` (vert/rouge = historique, pas une promesse). **Kill-safety 2 checkpoints + hold-lock** : signature OB 17 → `ensurePlayer` SEUL (pas le `Challenge` : son horloge jour-1 ne doit pas tourner derrière le paywall) + `ContractSnapshot` en UserDefaults ; paywall → `hasCompletedOnboarding` ; OB 21 → `hasFinishedPostPaywall` (le hold-lock : `RootView` garde la cover tant que le trio n'est pas fini). **PIÈGE `ensurePlayer` + seed** : fetch-or-create → le funnel rejoué récupérait l'OVR 61 seedé ; fix = `debugReplayOnboarding()` (vide TOUT sans recréer de joueur, arme seedDisabled, reset flags, annule la notif) + le seed marque l'onboarding fait. **OB 14 = DÉMO** : aucun `checkTask`, zéro OVR, zéro streak — la flamme "Day 0" est visuelle (brancher un vrai check casserait l'anti-farming). `HoldToConfirm` gagne `ringWidth` (anneau 148 pt d'OB 14). Divergences maquette tranchées côté moteur : "~78 · MASTER" → **WARRIOR** (Master ouvre à 80) · "CLASSIC" sur chip 30 d → `PresetCatalog.title` · "Hold for 1.5 seconds" → "Hold to check." (la constante vaut 1.0) · emojis → SF Symbols · "6:30 AM" → `reminderMinutes` (420). |
| 2026-07-15 | S5 : **`Rank.displayName` remonté en Core** (`SharedTypes`) — il y avait DÉJÀ deux copies locales qui avaient DÉJÀ divergé (`HomeViewModel` rendait "NOVICE", `ProgressionRankNaming` rendait "Novice"). Core = casse titre, Home `.uppercased()` à ses call sites d'affichage, les labels VoiceOver gardent la casse titre (mieux lu). Les 2 helpers locaux supprimés. Leçon : un helper "local à la feature" en attendant se duplique à la 3e feature — le remonter au 2e call site, pas au 3e. |
| 2026-07-15 | S5 previews : **UN SEUL `OnboardingPreviewFactory`** pour tout le module (`Features/Onboarding/OnboardingPreviewSupport.swift`, `#if DEBUG`) — chaque écran qui aurait son propre factory = N containers dans le process = le crash multi-container iOS 17. Le factory retient le container en `static let` (piège `mainContext` ne retient pas son container). Store DÉLIBÉRÉMENT vide (no player, no challenge) = le seul état contre lequel le funnel tourne en vrai. Fixtures `OnboardingDraft.previewAnswered/.previewHeavy/.previewLight` + `OnboardingPreviewChrome` (fond ink, et pour l'acte 0 la vidéo sous son scrim — `WelcomeScrim` extrait exprès : une preview de hook sans scrim ne montre rien de jugeable). Preview OB 15 passe `reviewPrompted = true` : le canvas ne doit pas déclencher la feuille de review système. |
| 2026-07-16 | S5b RESTRUCTURATION flow (actée, pattern RiteOff) : quiz → `loaderAnalysis` (loader narratif "ANALYZING YOUR ANSWERS", ex-loaderBuilding déplacé, stats orbitales SANS OVR ni durée, sortie = pilule "Access your report") → `report` (NOUVEAU `ReportScreen` = le SEUL écran dense autorisé : fight / screen time + shock / weak spot / targets / potential via `OnboardingCopy.reportRows`, OVR délibérément absent) → `diagnostic` (reveal) → `compose` → `projection` (reste après compose, la date a besoin de la durée ; le loader d'avant-projection remplacé par un beat "Locking…" 0,9 s dans ProjectionScreen). D1 intact. Ordre verrouillé par test ; barre = quiz+report+reveal (16 steps), masquée sur le loader ; `OnboardingLoaderScreen` ne sert plus qu'à OB 19. Amendement daté en tête d'ONBOARDING-PLAN.md (supersède l'ordre du corps). Batch device du jour aussi : back supprimé sur les REVEALS (06/07/10/13) · OB 13 recomposé (titre → carte dessous, centré) · blocs 06/09/10 centrés verticalement + tailles hook (Bebas 88 stat choc, tampon 38/56) · OB 09 séquencé 3 temps (goals → tampon Bebas heavy haptic → pivot) · OB 10 count-up 0,8 s + rouge éteint `accentMuted` (#9A4238, nouveau token) + rang tamponné · kickers TOUS supprimés (scaffold sans eyebrow) · OB 01a = sensei seul centré halo (strip abandonné après 2 passes) · OB 01c carte = `FudoGlassCard` teinte `surfaceGlassInk` (glassEffect .regular iOS 26, un tint BLANC sur grande surface glass = gris lavé, piège) · vidéos welcome à 0,5× (`defaultRate`, la couture suit en temps item) · onboarding = RACINE rendue de RootView (jamais une cover : fullScreenCover ANIME toujours sa présentation) · chips 30/60/90/120 + `monk120`, reco 60, chip 60 liseré `positive` quand non sélectionnée · OB 11 scrim CTA dégradé + caption courte · OB 06 réduit à 4 éléments (`shockOfYourLife` fusionnée). |
| 2026-07-16 | S5b 2e passe device. PIÈGE cover : `fullScreenCover` ANIME sa présentation même quand l'item est posé avant le 1er frame → l'onboarding montait du bas au launch. Fix : l'onboarding est la RACINE rendue de RootView tant qu'il est pending (`if hasCompletedOnboarding` — lu en synchrone à l'init), plus jamais une cover ; swaps mi-session (fin de funnel, replay DEBUG) = crossfade ; le paywall reste une cover, le shield ne couvre plus que ce gap-là. OB 01a strip refait (réf ob-01a) : passé = vraie silhouette (saturation 0.25 + brightness −0.18 + blur 4 EN DERNIER + opacity 0.38), alignement bottom commun, points = rampe taille+opacité+élévation vers le sensei net. Vidéos welcome à mi-vitesse : `player.defaultRate = OnboardingMetrics.videoRate` (0.5, plage 0.4-0.6) — l'observer de couture vit en temps ITEM donc le point de dissolve suit le rate gratuitement, la lane sortante gagne de la marge sous le fondu. `FudoGlassCard` (DesignSystem) = sibling card de FudoGlassCapsule : `glassEffect(.clear.tint(surfaceGlass))` iOS 26 (variante discrète — une carte flottante chuchote là où une barre système parle), fallback recette ultraThinMaterial, jamais d'ombre ; ProtocolGlassCard le consomme. |
| 2026-07-16 | Session S5b (retours device). BUG A : la décision onboarding-vs-Home attendait `onAppear` ET `fullScreenCover` présente en asynchrone → ~0,5 s de Home visible sous le funnel. Fix : route lue en SYNCHRONE dans `RootView.init` + `RouteShieldView` (fond + ensō) en overlay tant que `cover != nil` — le shield tombe avec le binding, la dismissal révèle le Home normalement. BUG B : `canAdvance(.compose)` était gaté sur `setup.canCommit` dont la clause "no active challenge" appartient au skin standalone (qui LANCE) — un défi qui traîne en base (état seed mi-S5 : données présentes + flags onboarding vides) = CTA OB 11 mort. Fix : `canCompose` (règles only) pour OB 11, `canCommit = canCompose && no-active` pour le launch. PRODUIT : chips 30/60/90/120, reco → monk60 (supersede D3 — TENSION ouverte : les hooks disent encore "30 DAYS", copy à arbitrer par Romain), `monk120` "Monk Mode 120" (protocole = Hardcore 90), `classic75` retiré du catalogue, `retiredTitles` garde son nom d'affichage. UX : barre de progression STABLE — `OnboardingChromeHeader` (chevron slot réservé + barre) rendu UNE fois par le flow container AU-DESSUS des écrans qui slident, les écrans gardent un slot vide 24+8 ; staggers quiz supprimés (le slide = la seule chorégraphie) ; OB 06 = chiffre + une ligne contexte + pivot (recut à 45 %, stake 30 j supprimé copy+test) ; OB 10 = "YOUR STARTING POINT" en Bebas display, eyebrow/title du scaffold devenus optionnels. |
| 2026-07-16 | S5c batch testeur #1 (9 retours actés Romain, amendement bis dans ONBOARDING-PLAN.md). **Split OB 11** : `composeDuration` (11a, durée seule, nouveau `ComposeDurationScreen`) → `composeRules` (11b, ex-ComposeProtocolScreen, caption "Pre-built for you. Edit anything.") — barre passe à 17 steps (dérivée de l'enum, test màj). **PIÈGE SwiftUI logué** : `sheet(isPresented:)` + `@State` optionnel = contenu du sheet évaluable contre la valeur STALE (nil) au 1er present → toujours `.sheet(item:)` avec un type Identifiable (`RuleSheetMode` add/edit, appliqué aux 3 skins règles). **OB 02 "multi-select"** : l'état était single, le RENDU mentait — désélection sur la courbe 0,5 s = 2 rows allumées en même temps ; fix = désélection snap 0,15 s (`OnboardingMetrics.optionDeselect`, exception assumée à la règle 0,4-0,6 s, même famille que quickLongPress). **Rows règles** : tap = toggle row ENTIÈRE, long-press = context menu natif Edit/Delete (`RuleRowEditor` gagne `onDelete`, la coche n'est plus un bouton). **OB 17 Clear** : vide strokes + `clearSignature()` (le fait signé est révoqué, CTA re-mort, test). **Quiz** : `AgeBracket.senior55plus` (pivot 60/horizon 70, "35+" → "35 — 54") · `Procrastination.everyDay` 0 pt en tête de `displayOrder` (plancher inchangé, barème INTACT — cas additif seulement). **REPORT accordéon** : détail replié par row, tap déplie ; chaque row DOIT porter un détail (test `everyReportRowCarriesADetailForTheAccordion`). Compile-only + build-for-testing verts ; Cmd+U + funnel device = Romain. |
| 2026-07-16 | S5c batch #2 (report v2 + compose, acté Romain — amendement ter ONBOARDING-PLAN.md). **+4 questions quiz** (wakeUp/training/focus après le shock, attempts avant reflection — enums UI-only dans OnboardingDraft, jamais l'OVR), barre à 22 steps. **REPORT v2** : 7 sections (héros + jauge YOU rouge/AVERAGE gris/TARGET vert + 1 ligne), accordéon 1re ouverte ; **TOUS les benchmarks dans `ReportBenchmarks.swift`** (moyennes publiques arrondies, garde-fou honnêteté verrouillé par test — jamais de percentile/« studies show », jamais un chiffre en dur dans une view) ; jauge écran = `ShockMath.hoursPerDay` passé internal (UN mapping). **Explainer** `protocolIntro` (1 phrase) entre reveal et 11a. **11a** : cards pleine largeur `DurationCard` (taglines re-coupées DANS PresetCatalog : The reset/The standard/For the strong/The ultimate — source unique, tous les skins), 60 pré-sélectionnée + badge vert + scale 1.03, header « How long? » seul. **11b** : « Your rules — pre-built for you » en 2 Texts baseline (JAMAIS Text+Text : Fonts figés = Dynamic Type mort) ; menu Edit/Remove. **PIÈGE Swift logué** : cas d'enum `none` assigné à un Optional (`draft.trainingLoad = .none`) résout `Optional.none` = nil, la réponse disparaît en silence → cas renommé `zero`, ne JAMAIS nommer `none` un cas porté par un Optional. Compile-only + build-for-testing verts ; Cmd+U + device = Romain. |
| 2026-07-16 | S5c batch #3 (interstitiel + report v3 + cards, amendement quater). **Beat `sixtySeconds`** entre 01c et le quiz : Bebas + anneau-chrono, AUCUN CTA, auto-advance 2,2 s — pattern exactly-once pour tout auto-advance : garde `fired` + `guard !Task.isCancelled` après le sleep (un `.task` démonté mi-beat ne doit JAMAIS tirer le advance). 01c : micro-ligne 60s supprimée, CTA « Build mine ». **Report v3** : BUG device — l'accordéon vivait SANS ScrollView dans le scaffold → les folds ouverts écrasaient le layout ; fix = card dans ScrollView (l'ouverture POUSSE) ; leçon : tout contenu expansible DOIT vivre dans un scroll. Nouveau token **`AppAnimation.spring`** (0.45/0.85, la seule spring — layout qui grandit sous le doigt only). Fold = texte 60 % | jauge 40 % ; flèche verdict ▲▼ (couleur SUR la flèche, règle palette) via `ReportGauge.youBeatsAverage` calculé dans ReportBenchmarks (sens par métrique, testé heavy/light). **Hardcore 90 renommé « Monk Mode 90 »** dans PresetCatalog (case `hardcore90` = identité data, affichage rétroactif). `DurationCard` : badge paramétrique + « X days » trailing ; 120 = « RECOMMENDED IF YOU'RE A KILLER » vermillon (vert unique à la 60). Compile-only + build-for-testing verts ; Cmd+U + device = Romain. |
| 2026-07-16 | S5c batch #4 (fix interstitiel, périmètre strict). Auto-advance REMPLACÉ par chorégraphie 3 temps : scale-in+chrono 1,7 s → « AND WE LOCK YOU IN » (Bebas, climax vermillon, haptic) → +0,4 s CTA « Lock me in » slide-up (variante « I'm in. » à trancher device). PATTERN rejouabilité acté : un écran à séquence one-shot expose `hasPlayed` + `onPlayed` et son état "joué" vit dans le VM (`sixtySecondsPlayed`, mémoire de session) — le back re-crée la view (@State repart à zéro), donc l'état final DOIT être posé à froid depuis le flag, jamais rejoué. Concat Bebas bicolore : `Text.foregroundStyle` par segment + UN `.fudoFont` sur le Text combiné (jamais de Font par segment = piège Dynamic Type). Compile-only + build-for-testing verts. |
| 2026-07-17 | S5c batch #5 (bug transition OB 13 + refonte projection). **PIÈGE SwiftUI logué** : un loader posé en `.overlay` SUR le contenu = un seul sous-arbre pour la transition du flow (`.move + .opacity`) → à l'insertion TOUT est semi-transparent, le fond « opaque » de l'overlay ne couvre plus rien, le headline fantôme glisse à travers (vu frame-par-frame device) ; et `withAnimation { loaderOff }` + reveal dans la même passe = 2 phases visibles. Fix = **state machine 3 phases** dans ProjectionScreen (`locking` pilule glass seule, contenu ABSENT de la hiérarchie — jamais opacity 0 → `blank` : le fade-out du loader FINIT avant toute entrée → `reveal` : headline fade+rise, courbe draw-in, CTA en dernier via `ctaVisible` nouveau param scaffold). Barre : `viewModel.showsChrome` = `step.showsProgress` + exception dynamique beat locking (les loaders ne portent jamais la barre) — le flow gagne `.animation(value: showsChrome)`. Rejouabilité batch #4 : `projectionPlayed` (VM) + état settled posé À L'INIT (pas dans run() — zéro frame de loader au re-entry). **Refonte OB 13** : date héros Bebas (ON petit / SEPTEMBER 14 géant 58 / « you'll be at ~91 » vermillon + chip rang `accentDeep`), courbe = rendu COMPLET masqué par `scaleEffect(x:, anchor: .leading)` (vrai draw-in gauche→droite, tout ce qui roule sur la ligne suit), gradient vermillon sous la courbe (AreaMark), ticks de rang aux `Rank.floorOVR` croisés (jamais de seuil en dur, dernier jour exclu = le dot final le porte), pulse endpoint via `chartOverlay` + `proxy.plotFrame` (le pulse est gaté par `landed` séparé de `progress` : la VALEUR d'un state animé est posée dès le départ, seule la présentation interpole), micro-ligne « Exact date. Real number. No motivation quotes. ». Beats dans `OnboardingMetrics.Projection`. Compile-only + build-for-testing verts ; funnel device ×3 = Romain. |
| 2026-07-17 | Batch #5 retours device (courbe OB 13). Labels de rang `.bottom` = posés SUR la ligne → **`.topLeading`** (au-dessus-gauche d'un point d'une courbe croissante = toujours vide) + `overflowResolution: .fit(to: .plot)` en x (le 1er tick ne saigne plus hors bord gauche). **Tick du rang FINAL exclu** (le chip headline + le dot final le portent — 3e marqueur collé au pulse = bruit). Dots coupés aux bords → `.chartXScale(range: .plotDimension(padding: 10))` (inset du plot, le pulse tient dans le frame ; `proxy.position` suit le range automatiquement). **Courbe re-pacée pour l'œil** (demande Romain « moins uniforme ») : deltas quotidiens d'`OVREngine.project` modulés par des sinus composés (~4 vagues quelle que soit la durée, phase seedée par `base`) puis RE-NORMALISÉS — total + extrémités = moteur au centième, monotone par construction (amplitude < 1), déterministe OBLIGATOIRE (computed property recalculée à chaque render : du random = courbe qui frétille). Le moteur reste intact, seul le rythme jour-à-jour est un habillage visuel. Compile-only vert ; device = Romain. |
| 2026-07-23 | Session S6 RevenueCat + paywall (PRD 03). SPM `purchases-ios-spm` 5.81.1 ajouté À LA MAIN dans le pbxproj (4 objets : packageReferences projet + XCRemoteSwiftPackageReference + XCSwiftPackageProductDependency + PBXBuildFile phase Frameworks) — les .swift restent auto-inclus (synchronized groups). `AppConfig` (App/) = maison unique clé publique RC, `proEntitlementID`, `defaultOfferingID`, URLs legal (Settings re-pointe dessus). `EntitlementStore` (Core/Services) = SEULE couture RC : les views/VMs ne voient que `PaywallPlan` (display model RC-free, prix localisés StoreProduct, trial lu sur l'introductoryDiscount) + outcomes ; `customerInfoStream` → achat/expiration/restore reflétés sans relaunch ; gating RootView : `entitlementActive = isResolved ? isPro : true` (non-résolu = PRO : un payant ne voit JAMAIS flasher le paywall, le cache RC résout dans les premiers frames même offline ; l'expiré a le cover un battement plus tard). Badge "SAVE 86%" CALCULÉ (`savingsPercent`, testé) jamais en dur ; timeline trial seulement si le plan SÉLECTIONNÉ a un trial (Annual → ligne "Billed today") ; social proof = rien (D4). `PaywallGateView` = même signature, délègue à `PaywallView` (X funnel préparé via `onClose` optionnel, NON branché — session onboarding). DEBUG sandbox bloqué : override System/Pro/Free persisté (`debug.proOverride`) + mocks produits sur l'état failure + PIÈGE : Free couvre TOUTE l'app dont Settings → l'escape "Reset override" vit SUR le paywall. PIÈGE shell : `xcodebuild \| tee \| grep \| head` = head ferme le pipe → SIGPIPE tue le build EN COURS (log tronqué sans verdict) — toujours `> fichier 2>&1` puis inspecter ; zsh = `$pipestatus` pas `$PIPESTATUS`. Achats non testables (bancaire) : à vérifier sandbox plus tard — produits attachés à l'entitlement `pro` dashboard + trial 3 j ASC sur le weekly. |
| 2026-07-23 | S6-v2 (retours device). **Mock CTA** : `useMockPlans()` arme `isMockSession` → l'achat SIMULE le vrai chemin (beat in-flight 0,9 s → `debugProOverride = true` → même `finishUnlocked()` que le réel) — funnel → Welcome → Home day 1 testable sans sandbox ; retour arrière = override System dans Settings. **PIÈGE SwiftUI logué** : `.animation(value:)` sur un swap d'états exclusifs (switch skeleton/échec/loaded) = crossfade où les DEUX sous-arbres passent semi-transparents un battement → « frame vide » perçu device ; états exclusifs = swap SEC, on n'anime que les changements DANS un état. **Redesign paywall** (bench Duolingo/Cal AI, screenshots) : 3 bullets USP SF Symbols vermillon (jamais de tableau free/premium — perdant documenté) · timeline = spine vermillon CONTINUE, dernier segment + beat billing fondus (futur) · cards : prix héros `stat(22)`, badge coin « 3 DAYS FREE » vermillon SUR l'arête (le mot FREE > le prix), « SAVE 86% » vert coin annual, liseré sélection 2 pt · « ✓ No payment due now » au-dessus du CTA (trial only) · sous CTA : reprise compacte `priceRecapLine` + `autoRenewNotice` statique (2 lignes courtes, remplace complianceLine-pavé). **X funnel branché** : `closePaywall()` VM (pas `back()` — showsBack false sur paywall) → retour contrat signé. Trial toujours porté par le Weekly (spec S6) ; toute l'UI (timeline, badge, CTA, no-payment, recap) suit LE PLAN À TRIAL — si le trial migre sur l'Annual un jour, seuls les produits/mocks changent. |
| 2026-07-23 | S6-v3 éligibilité trial : `EntitlementStore.trialEligibleIDs` = `checkTrialOrIntroDiscountEligibility` (RC) au `loadPlans` — un `.ineligible` explicite construit le plan avec `trialDays: nil` À LA SOURCE, et TOUTE l'UI trial (badge coin, timeline, CTA « Continue », no-payment line, recap « billed today ») suit sans un seul if de vue ; `.unknown` = optimiste (pas de reçu ≈ nouveau user, ne jamais sous-vendre un éligible). DEBUG `debugTrialConsumed` persisté (`debug.trialConsumed`, toggle Settings §DEBUG) : force l'inéligibilité sur les plans réels ET les mocks (`PaywallPlan.strippingTrial()`, miroir testé du gate) — layout inéligible testable sans reçu ni sandbox. Leçon confirmée : tout état « promesse » cuit dans le display model (pas dans les vues) = un fix d'éligibilité tient en un gate. |
| 2026-07-23 | Batch #12. BUG paywall-X : l'état d'OB 17 (strokes + sealed) vivait en @State de vue → recréation = contrat vierge ; fix = hissé dans le VM (`signatureStrokes`, `contractSealed`, session memory) + `startsSealed` devient le chemin de reprise (état final posé à froid, pattern rejouabilité batch #4). 11b : `RulePersonalizer` (onboarding only) = catalogue complet visible, 4 règles préselectionnées par affinité aux réponses (match par iconName, stable, déterministe) — appliqué par `preparePersonalizedRules()` idempotent par preset (changer la durée re-personnalise = sémantique chip) ; cap 8 déplacé sur les règles ACTIVES (`canAddRule`/`toggleRule` gardent au enable, skins partagés OK) ; jauge sweet-spot `RuleLoadGauge` (3-5 vert · 6 vermillon · 7-8 rouge) remplace le warning isolé. OB 07 : +2 goals (`doWhatIveWanted` acté, `moreEnergy` proposé) + `Goal.displayOrder(for:)` tri par affinité (réponses ANTÉRIEURES au screen only). PIÈGE Xcode logué : 31 fichiers « Foo 2.swift » (doublons byte-identiques générés par Xcode, dossiers synchronisés) cassaient le build (types ambigus) — déplacés au scratchpad, jamais supprimés en autonomie ; après nettoyage, erreurs fantômes de macro @Observable = artefacts stale, un rebuild suffit. |
| 2026-07-23 | S6-v4 (conflit #25 acté) : **trial 3 j sur LES DEUX plans** (offre ajoutée au yearly en ASC). Mock annual corrigé **44,99 $** (l'ancien 43,99 était un bug de mock, les produits RC étaient bons) + `trialDays: 3` + per-month 3,75 $ — SAVE % toujours CALCULÉ, 5,99×52 vs 44,99 → 86 % inchangé (verrouillé test). **Arbitrage badges** : UN badge coin par card, jamais deux qui se battent — le trial ne différencie plus (il est partout), donc coin annual = « SAVE 86% » vert (l'argument de tête) et son trial parle par le sous-titre « FREE for 3 days » + « $3.75 per month » EMPILÉS (les deux Texts émis dans le VStack parent), la timeline, le CTA et le recap ; coin weekly = « 3 DAYS FREE » vermillon. Annual sélectionné par défaut → la timeline Blinkist est désormais visible d'entrée. `strippingTrial()` reste le chemin de test inéligible (les tests no-trial passent par lui, plus par le mock nu). |
| 2026-07-15 | Previews Xcode + SwiftData, DOUBLE piège (canvas HomeView) : (1) depuis Xcode 15 la preview lance la VRAIE app → FUDOApp construisait son container réel en plus de celui du preview = crash multi-container connu ; fix = coquille vide sous `XCODE_RUNNING_FOR_PREVIEWS` (même pattern que les sessions de test). (2) `container.mainContext` ne RETIENT PAS son ModelContainer : container local de closure → désalloué → SwiftData reset le contexte → "Fatal Error in BackingData.swift / destroyed by ModelContext.reset" ; fix = le factory garde le container en `static let` (raison pour laquelle FUDOApp stocke `private let container`). Pattern acté pour tout futur preview : `XxxPreviewFactory` (enum @MainActor, `static let container` + `static let store`, in-memory sur `FudoSchema.schema`, seed via `DebugSeed.seed`), bloc `#if DEBUG` en bas du fichier de la view. |
| 2026-07-23 | Session P7 WIDGETS (small + medium, différenciateur — aucun concurrent n'en ship). Target `FUDOWidgetExtension` + App Group `group.com.fudoapp.fudo` déjà posés à la main par Romain (dans les 2 `.entitlements`, câblés build settings). Le widget lit UNIQUEMENT un `WidgetSnapshot` JSON (App Group UserDefaults) — jamais SwiftData, jamais RC/PostHog (packageProductDependencies du target VIDE). **Bridge** : `WidgetSnapshot.swift` (Core/Services) = DTO Codable + suite + load/save Foundation-only, **source unique partagée app+widget** ; `WidgetBridge.swift` (app only, importe WidgetKit) construit le snapshot depuis GameStore + `reloadAllTimelines()` SEULEMENT si le blob a changé (byte-equality). **Single write site = `GameStore.save()`** : couvre check/uncheck/rollover/start/abandon/edit/erase/decay en 1 point ; no-player → snapshot effacé (état vide). `widget_detected {families}` fired depuis `RootView.refresh()` (foreground) via `getCurrentConfigurations`, seulement si le set de familles change (persisté standard UserDefaults) ; DEBUG = Noop. **PIÈGE pbxproj RÉSOLU + VÉRIFIÉ** : partager 1 fichier entre 2 synchronized groups = ajouter un `PBXFileSystemSynchronizedBuildFileExceptionSet` `target=<autre target>` + `membershipExceptions=(<path relatif au folder>)` dans les `exceptions` du root group propriétaire → **INCLUT** le fichier dans l'autre target (miroir inverse de l'exclusion Info.plist ; `membershipExceptions` = seule clé de membership, flippe le défaut par target). Confirmé au build (WidgetSnapshot compile dans les 2 targets). **PIÈGE stubs template** : `FUDOWidgetControl.swift` (ControlWidget) exige **iOS 18** → cassait le build à 17.0 ; les stubs Control + LiveActivity = junk hors scope, VIDÉS (comment-only, compile, ship rien) car suppression = commande servie à Romain (`rm FUDOWidget/FUDOWidgetControl.swift FUDOWidget/FUDOWidgetLiveActivity.swift`). **Design** (reco Romain, PAS d'avatar sensei) : anneau ensō vermillon (progression jour X/Y) + OVR géant mono + rang + pilule flamme ; medium = hero gauche + 3 tâches restantes (SF Symbol + titre) / « Day X sealed » gold si 0 restante ; entre défis = rang+OVR+« New challenge? » ; vide = marque ensō/flamme + « Start your monk mode. » Couleurs = colorsets miroir dans `FUDOWidget/Assets.xcassets` (mêmes noms que FudoColor) via `WidgetPalette`. Timeline = snapshot now + refresh à minuit+5 min. Compile-only app+extension = **BUILD SUCCEEDED** ; rendu 2 tailles + tap→app = Cmd+R Romain. |
| 2026-07-23 | Session S9 NOTIFICATIONS locales (6 notifs, extension du `NotificationService` S5 — jamais de scheduler parallèle). **Architecture** : `NotificationCopy.swift` = TOUTE la copy + les 6 `Kind` (rawValue = id de request ET slug analytics, une seule string) + clés userInfo + deep link, source unique pour la session ASO. `NotificationService.Planner` = brain PUR (value in/out, zéro appel système) → testé (`NotificationPlannerTests`, 17 cas) ; seul `install()` touche le center (no-op total sous test/preview via `isSideEffectsSuppressed` env-guard). **Deux formes de travail** : (1) RECOMPUTE (`recompute(from:)`) = le set conditionnel du jour (daily/evening/streak/decay), reconstruit à CHAQUE `GameStore.save()` — MÊME seam que WidgetBridge, ajouté juste après ; un check qui complète le jour efface les nudges du soir, un rollover réarme demain. (2) EVENT-SCHEDULED = trial (au purchase) + rank-up (au gain) — jamais touchés par recompute (leurs ids hors `managedIDs`). **Invariants du planner** : jour 100 % = ZÉRO notif ce jour (daily → demain seulement) · daily CONDITIONNEL (droppé si jour fait) · CAP DUR 2/j (daily compte, puis evening OU streak, jamais les 3 : `budget = dailyEnabled ? 1 : 2`) · evening (20:30) sauté si reminder > 20:30 · streak (22:30) seulement si streak ≥ 1 · decay = jour 7 après `lastDayClosedAt` à midi, uniquement SANS défi (avant le 1er tick qui est J10). **Toggles vrais** (étaient no-op S10) : `eveningReminders` gate 2+3, `rankCelebrations` gate la notif rank-up immédiate, `dailyReminder` gate 1 — recompute lit `NotificationPreferences` frais (opt-out) ; SettingsVM route via recompute (plus de `scheduleDailyReminder` direct). **Trial D-1** : `scheduleTrialEndingReminder(trialDays:)` remplace le stub S6, câblé dans `finishUnlocked(trialDays:)` (purchase réel + mock ; restore = nil = pas de notif) ; 24 h avant fin = J(trialDays−1) ; IGNORE les toggles (transparence billing, non négociable). **Rank-up** : `postRankUpIfBackgrounded` depuis `raiseRankMarkIfNeeded` — self-gate `applicationState != .active` (foreground → RankUpCover) + toggle ; tap → deep link. **Deep link** (App entry only) : `AppDelegate` (@UIApplicationDelegateAdaptor) = délégué UNUserNotificationCenter → fire `notification_tapped {id}` PUIS route via `NotificationRouter.shared` (singleton @Observable, seul pont UIKit→SwiftUI) ; RootView draine `pendingDeepLink` (onChange + refresh pour le cold-launch), consomme le mark rank-up AVANT de présenter la share card (`.shareCardPreview`, variant `.rankUp`) → cover et share ne se battent jamais. `notification_tapped` ajouté à AnalyticsEvent. **PIÈGE daily conditionnel** : iOS n'a pas de trigger conditionnel → daily passe de repeating (OB18, écran UI non modifiable, transitoire) à one-shot recompute-managé (même id `fudo.daily.reminder`, écrasé au challenge start). Un app jamais rouvert se tait après le prochain one-shot = tradeoff assumé (fidèle au modèle "recompute on every event"). `eraseAllData`/`debugReplayOnboarding` → `cancelAll()` (les 6 ids). Compile-only app + build-for-testing = **BUILD SUCCEEDED** ; Cmd+U + funnel device (plan DEBUG intervalles courts) = Romain. Ouvert : copy trial "OVR up in 2 days" littérale (pas de valeurs réelles au purchase, défi pas encore créé) ; decay non gaté par un toggle (aucun des 3 ne le couvre — à trancher si besoin). |
| 2026-07-23 | S9 micro-pass (validée chef). (1) Copy `trial_d1` → « Your trial ends tomorrow. Keep your OVR climbing? ». (2) `decay_warning` gaté par le toggle `dailyReminder` (`trial_d1` reste la SEULE notif toggle-immune) — gate dans la branche no-challenge du planner, testé (`decayGatedByDailyToggle`). (3) Bouton DEBUG « Post rank-up notif » (menu debug Settings) → `NotificationService.debugPostRankUp` (bypass app-state + toggle, delay 4 s pour attraper la bannière en verrouillant) ; partage le `rankUpRequest(delay:)` avec le post live ; permet de tester tap → share card sur device (un vrai gain n'arrive jamais en background). Compile-only + build-for-testing = **BUILD SUCCEEDED**. |
| 2026-07-23 | P7 retours device #1 (Cmd+U vert). Dark + light OK d'entrée (couleurs forcées tiennent). **PIÈGE widgetRenderingMode** : en `.accented` (teinté) et `.vibrant` (transparent) iOS JETTE les couleurs custom (rendu par luminance) et transforme tout **fill plein en blob opaque** → la pilule flamme devenait un aplat. Fix : (1) fill de la pilule (Capsule `bgCard`) SEULEMENT en `.fullColor` (`@Environment(\.widgetRenderingMode)`, sinon flamme+chiffre nus) ; (2) `.widgetAccentable()` sur les glyphes héros (arc d'anneau, flamme, icônes de tâches, seal, marque vide) → ils prennent la teinte user, le texte reste dans le groupe défaut ; le track d'anneau NON-accentable = contraste avec l'arc. **PIÈGE layout** : anneau small en overlay ZStack chevauchait « DAY X/Y » et la pilule → passé en **VStack empilé** (flame row / anneau `maxHeight:.infinity` / day row), plus jamais de clip. Confirmé device : check→update widget, abandon→New Challenge, tap→app, tous OK. Idée ouverte Romain : mini-graphe OVR dans le widget (nécessite d'ajouter un historique court au `WidgetSnapshot` — non fait, à trancher). |
| 2026-07-23 | Session S11 FIN DE DÉFI + boucle inter-défis. **Beaucoup était déjà là** : `completeChallenge` (marque `.completed` + `endOVR` + count++, PlayerState INTACT), decay déjà câblé dans `processRolloverIfNeeded` (branche no-challenge), `ShareCardData.challengeEnd`, `FudoCover.challengeComplete` (case réservée), `StatsAggregator.missedCount` par règle. **Construit** : signal `GameStore.pendingChallengeCompletion` (analogue `pendingRankUp`) posé dans `completeChallenge` → RootView le DRAINE dans un état local `CompletionFlowStep` (`.verdict`/`.setup`). **PATTERN acté** : verdict→setup vivent sur UNE SEULE `fullScreenCover(item:)` (l'enum change de case = swap de contenu, JAMAIS un dismiss+re-present = zéro race de présentation) ; `drainCompletion()` appelé dans `refresh()` (rollover) ET `onChange(pendingChallengeCompletion?.id)` (chemin DEBUG hors refresh), no-op si un flow tourne déjà. **Contention rank-up** : la séquence ABSORBE le rank-up du dernier jour (beat 1 rejoue le sensei start→end) → `completeChallenge` met `pendingRankUp = nil` + le binding rank-up de RootView est gaté sur `pendingChallengeCompletion == nil`. **`ChallengeCompletionSummary`** (Core/Game, value type aplati) = faits purs, builder `make(...)` prend des primitives (jamais un `@Model`) → testé SANS container ; GameStore aplatit le graphe (jours clos, misses/règle via `TaskRule.id`, `reusedRules`). La logique next-hook (preset supérieur, ajouts restart) vit côté Features (lit PresetCatalog) — le summary Core ne fuit pas la couche. **Décisions Romain** : cover = 3 PAGES successives (verdict count-up + replay sensei si rang gagné / carte partage `challengeEnd` avec Share primaire + retry / hook « A Warrior doesn't stop at 76 » → Next challenge + Restart harder + « Maybe later ») · **Restart harder** = mêmes règles (converties `.plain` pour garder les titres cuits) + `PresetCatalog.hardenAdditions` (≤2 règles du protocole standard non prises, cap sur `maxRules` sinon CTA setup MORT) ; règle la plus ratée CITÉE dans le copy, jamais durcie · CTA → Setup PRÉ-REMPLI (pas de launch instant). **superiorPreset** ladder 30→60→90→120 (cap ; 75→90). **ChallengeSetupViewModel** gagne `initialPreset`/`initialRules`/`origin` (découplés du label « recommended ») ; `origin: .postChallenge` enfin utilisé (était mort) → `challenge_started {origin: post_challenge}`. **decay_started** sur le 1er tick de CHAQUE épisode idle (`ticksApplied == 0`, PAS un nil-check : un `lastDecayTickAt` périmé d'un épisode antérieur donne 0 → le nouvel épisode fire). **challenge_abandoned** dans `abandonChallenge` (couvre aussi le bouton Settings S10) — abandon ne pose JAMAIS `pendingChallengeCompletion`, zéro collision. **PIÈGE test logué** : un fichier de test qui fait `.init()` pour un UUID a besoin de `import Foundation` — `import Testing` seul ne suffit pas (« initializer 'init()' is not available due to missing import of defining module 'Foundation' »). **Vérifié zéro code** : Home no-challenge affiche l'OVR live (decay reflété), widget idem, `decay_warning` déjà câblé (recompute dans `save()`), Progression past challenges montre completed ET abandoned. **Piège tranché (défaut Romain)** : app fermée 9+ j APRÈS fin → verdict montre l'`endOVR` gelé, Home affiche déjà l'OVR décrue (réaliste). Orphelin `CompletionPlaceholderView.swift` → suppression servie à Romain. Compile-only app+widget + build-for-testing = **BUILD/TEST SUCCEEDED** ; Cmd+U + funnel device = Romain. |
